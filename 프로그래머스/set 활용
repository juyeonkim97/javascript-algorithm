//프로그래머스 폰켓몬, 14분
function solution(nums) {
    let answer = 0;
    let limit=nums.length/2;
    let monster=new Set();
    nums.forEach(item => monster.add(item))
    if(monster.size>limit) answer=limit;
    else answer=monster.size;
    return answer;
}
